BÀI TẬP LỚN 1: SHERLOCK - A STUDY IN PINK (PHẦN 1)Môn học: Kỹ thuật Lập trình (CO1027)Ngôn ngữ: C++1. QUY ĐỊNH CHUNGGiới hạn chỉ số:$HP \in [0, 666]$. Nếu tính toán vượt quá, đưa về biên. 1$EXP \in [0, 600]$. Nếu tính toán vượt quá, đưa về biên. 2$M \in [0, 3000]$. Nếu tính toán vượt quá, đưa về biên. 3Quy tắc làm tròn: Mọi kết quả số thực tính toán cho $HP, EXP, M$ phải được làm tròn lên (ceil) ngay lập tức trước khi thực hiện các bước tiếp theo. 4444+1Tham số lỗi: Nếu mã sự kiện $E$ nằm ngoài khoảng $[0, 99]$, hàm trả về -99 và không thực hiện gì thêm. 5555+12. CÁC NHIỆM VỤ CHI TIẾTNhiệm vụ 1: Cuộc gặp gỡ đầu tiên (firstMeet)Chữ ký hàm: int firstMeet(int &exp1, int &exp2, int e1); 6Logic xử lý:Trường hợp $e1 \in [0, 3]$: Cập nhật $EXP_2$ (Watson) dựa trên bảng giá trị: 7$e1=0$: $+29$$e1=1$: $+45$$e1=2$: $+75$$e1=3$: $+149$ (tổng 3 thông tin)Cập nhật $EXP_1$ (Sherlock): Tính $D = e1 \times 3 + exp1 \times 7$. 8Nếu $D$ chẵn: $exp1 = exp1 + D/200$. 9Nếu $D$ lẻ: $exp1 = exp1 - D/100$. 10Trường hợp $e1 \in [4, 99]$: Cập nhật $EXP_2$ theo các mốc $e1$ và điều kiện tích lũy (Thông tin 1-5). 11Nếu giải thích đủ cả 3 thông tin ($e1 \in [80, 99]$ và thỏa điều kiện $EXP_2 > 400$): $exp2 = exp2 \times 1.15$. 12121212+1Kết quả: Trả về exp1 + exp2. 13Nhiệm vụ 2: Truy tìm hành lý (traceLuggage)Chữ ký hàm: int traceLuggage(int &hp1, int &exp1, int &m1, int e2); 14Logic xử lý: Phải đi qua cả 3 con đường để tính xác suất tìm thấy vali ($P_1, P_2, P_3$). 15Con đường 1: Gọi $S$ là số chính phương gần $exp1$ nhất. 16Nếu $exp1 \ge S$: $P_1 = 100\%$. 17Nếu không: $P_1 = ((exp1/S) + 80) / 123$. 18Con đường 2: Thực hiện chuỗi sự kiện (Mua thức ăn/nước, Thuê xe, Giúp người vô gia cư). 19Nếu $e2$ lẻ: Lặp lại các sự kiện cho đến khi tổng chi phí $> 50\%$ số tiền ban đầu. 20Nếu $e2$ chẵn: Thực hiện tối đa một lượt. 21Con đường 3: $P_3$ lấy từ mảng cố định $\{32, 47, 28, 79, 100, 50, 22, 83, 64, 11\}$ dựa trên chữ số hàng đơn vị của tổng các chữ số của $e2$. 22222222+1Tổng kết: Tính xác suất trung bình $P$. Cập nhật $hp1, exp1$ dựa trên $P$ (ngưỡng 50% hoặc trường hợp đặc biệt $P_1=P_2=P_3=100\%$). 23Nhiệm vụ 3: Đuổi theo taxi (chaseTaxi)Chữ ký hàm: int chaseTaxi(int &hp1, int &exp1, int &hp2, int &exp2, int e3); 2424+1Logic xử lý:Khởi tạo ma trận $10 \times 10$. Tính điểm Taxi tại $(i, j)$ bằng: ((e3 * j) + (i * 2)) * (i - j). 25252525+1Tính điểm Sherlock/Watson bằng giá trị max của các phần tử trên đường chéo trái và phải đi qua vị trí tương ứng. 26Tìm điểm gặp nhau $(i, j)$: $i$ là số lượng điểm Taxi $> e3 \times 2$, $j$ là số lượng điểm Taxi $< -e3$ (nếu $>1$ chữ số thì cộng dồn các chữ số). 27So sánh $|Taxi|$ và $|Sherlock+Watson|$ tại $(i, j)$ để quyết định tăng/giảm $12\% EXP$ và $10\% HP$. 28Nhiệm vụ 4: Kiểm tra mật khẩu (checkPassword)Chữ ký hàm: int checkPassword(const char *s, const char *email); 29Quy tắc hợp lệ: 30Độ dài $[8, 20]$.Ký tự: chữ cái, chữ số, hoặc đặc biệt (@, #, %, $, !).Không chứa se (chuỗi trước @ trong email).Không có quá 2 ký tự giống nhau liên tiếp (VD: aaa là vi phạm).Phải có ít nhất 1 ký tự đặc biệt.Mã lỗi trả về: -1 (ngắn), -2 (dài), -300-sei (chứa se), -400-sci (lặp ký tự), -5 (thiếu ký tự đặc biệt), -10 (hợp lệ). 31Gợi ý cho AI Agent: "Hãy thực hiện các hàm này theo đúng thứ tự logic, lưu ý đặc biệt việc ép kiểu số thực và làm tròn lên (ceil) cho các chỉ số sức khỏe và kinh nghiệm."